<?xml version="1.0" encoding="utf-8"?>
<!-- Основной контейнер экрана плеера (Альбомная ориентация) -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:padding="8dp">

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_poster_end"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.42" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_playlist_start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.45" />

    <!-- Левая часть: Карточка с обложкой и информацией о треке -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="8dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintBottom_toTopOf="@+id/details_title"
        app:layout_constraintEnd_toStartOf="@+id/guideline_poster_end"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/details_poster"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="@string/music_poster_content_description"
                tools:src="@tools:sample/avatars" />

            <ProgressBar
                android:id="@+id/player_progress_bar"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="center"
                android:visibility="gone"
                android:indeterminateTint="@android:color/white" />

            <ImageButton
                android:id="@+id/btn_lyrics"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="top|start"
                android:layout_margin="4dp"
                android:background="@drawable/bg_circle_translucent"
                android:padding="6dp"
                android:contentDescription="@string/show_lyrics"
                app:srcCompat="@drawable/ic_lyrics"
                app:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/btn_reminder"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="top|end"
                android:layout_margin="4dp"
                android:background="@drawable/bg_circle_translucent"
                android:padding="6dp"
                android:contentDescription="@string/remind_later"
                app:srcCompat="@drawable/ic_notifications_none"
                app:tint="@android:color/white" />
        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <!-- Текстовая информация: Название трека -->
    <TextView
        android:id="@+id/details_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Headline"
        android:textSize="16sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintBottom_toTopOf="@+id/details_artist"
        app:layout_constraintEnd_toEndOf="@+id/cardView"
        app:layout_constraintStart_toStartOf="@+id/cardView"
        app:layout_constraintTop_toBottomOf="@+id/cardView"
        tools:text="Track Title" />

    <!-- Текстовая информация: Исполнитель -->
    <TextView
        android:id="@+id/details_artist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/cardView"
        app:layout_constraintStart_toStartOf="@+id/cardView"
        app:layout_constraintTop_toBottomOf="@+id/details_title"
        tools:text="Artist Name" />

    <!-- Вертикальный блок функциональных кнопок (Избранное, Таймер, Поделиться, Скачать) -->
    <LinearLayout
        android:id="@+id/action_buttons_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginStart="15dp"
        app:layout_constraintStart_toEndOf="@+id/cardView"
        app:layout_constraintTop_toTopOf="@+id/cardView">

        <ImageView
            android:id="@+id/details_favorite"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginBottom="8dp"
            android:contentDescription="@string/favorite_icon_content_description"
            android:src="@drawable/ic_star_border"
            app:tint="@color/player_icon_tint" />

        <ImageButton
            android:id="@+id/btn_sleep_timer"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_marginBottom="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/sleep_timer_content_description"
            android:src="@drawable/ic_alarm_off"
            app:tint="@color/player_icon_tint" />

        <ImageButton
            android:id="@+id/btn_share"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_marginBottom="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/share_content_description"
            android:src="@drawable/ic_menu_share"
            app:tint="@color/player_icon_tint" />

        <ImageButton
            android:id="@+id/btn_download"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/download_content_description"
            android:src="@drawable/ic_download"
            app:tint="@color/player_icon_tint" />

        <ImageButton
            android:id="@+id/btn_delete_player"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/delete_track_title"
            android:src="@android:drawable/ic_menu_delete"
            android:visibility="gone"
            app:tint="@color/player_icon_tint" />
    </LinearLayout>

    <!-- Правая часть: Управление воспроизведением и Плейлист -->
    <SeekBar
        android:id="@+id/seek_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/guideline_playlist_start"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Текущее время воспроизведения -->
    <TextView
        android:id="@+id/tv_current_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="00:00"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@+id/guideline_playlist_start"
        app:layout_constraintTop_toBottomOf="@+id/seek_bar" />

    <!-- Общее время трека -->
    <TextView
        android:id="@+id/tv_total_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:text="00:00"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/seek_bar" />

    <!-- Блок кнопок управления воспроизведением -->
    <LinearLayout
        android:id="@+id/controls_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/guideline_playlist_start"
        app:layout_constraintTop_toBottomOf="@+id/tv_current_time">

        <ImageButton
            android:id="@+id/btn_prev"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="Previous"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_previous" />

        <ImageButton
            android:id="@+id/btn_play_pause"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginHorizontal="12dp"
            android:contentDescription="Play or Pause"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:src="@android:drawable/ic_media_play" />

        <ImageButton
            android:id="@+id/btn_next"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:contentDescription="Next"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_next" />
    </LinearLayout>

    <!-- Список очереди воспроизведения (Плейлист) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_small_playlist"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="4dp"
        android:orientation="vertical"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/guideline_playlist_start"
        app:layout_constraintTop_toBottomOf="@+id/controls_layout"
        tools:listitem="@layout/item_small_track" />

    <!-- Контейнер для отображения текста песни -->
    <ScrollView
        android:id="@+id/lyrics_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="4dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/guideline_playlist_start"
        app:layout_constraintTop_toBottomOf="@+id/controls_layout">

        <TextView
            android:id="@+id/tv_lyrics"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:gravity="center"
            android:textSize="14sp"
            android:textColor="?attr/colorOnSurface"
            tools:text="Song lyrics will appear here..." />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
